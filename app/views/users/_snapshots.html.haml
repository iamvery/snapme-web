%ul.thumbnails.snapshots
  - users.each do |user|
    %li.user{ id: user.id }
      .thumbnail
        - if user.snapshot.file.exists?
          = image_tag user.snapshot.url, class: :snapshot
        .caption= user.username
