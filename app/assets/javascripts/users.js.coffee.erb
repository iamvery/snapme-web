pusher  = new Pusher('<%= ENV['PUSHER_KEY'] %>')
channel = pusher.subscribe('<%= User::PUSHER_CHANNEL %>')
channel.bind '<%= User::PUSHER_NEW_SNAPSHOT_EVENT %>', (data) ->
  snapshot = $(".user##{data.user_id}")
  snapshot.
    find('img.snapshot').
    attr('src', data.snapshot_url)
